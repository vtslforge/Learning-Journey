const path = require('path')
// Exposes the project root so files can be resolved from a consistent location.
module.exports = path.join(__dirname, "..");

// basically gets the root directory of your main Node.js file and exports it.
